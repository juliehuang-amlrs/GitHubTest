\# Input Folder

\## Purpose

This folder stores input files used by the application, such as CSV, Excel, JSON, or XML files.

\## Git Policy

The `.gitignore` file is configured to ignore all data files in this folder. 

This helps prevent large files, sensitive information, and temporary test data from being committed to GitHub.

Only this `KEEP.md` file is tracked to preserve the folder structure.

